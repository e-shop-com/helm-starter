apiVersion: v1
kind: Service
metadata:
  name: {{ template "starter.fullname" . }}
  labels:
    app: {{ template "starter.name" . }}
    chart: {{ template "starter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "starter.name" . }}
    release: {{ .Release.Name }}
